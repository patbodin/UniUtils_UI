/* eslint-disable */
import ModalPopup from './ModalPopup';

export default {
  title: "ModalPopup",
};

export const Default = () => <ModalPopup onClose={null} show={false}/>;

Default.story = {
  name: 'default',
};
